#include <stdio.h>

/**
 * main -Entry point
 *
 * Description: program prints all single numbers
 * of bas 10 starting from 0 followed by new line
 *
 * Return: Always 0 (success)
 */

int main(void)
{

	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	putchar('\n');
return (0);
}
